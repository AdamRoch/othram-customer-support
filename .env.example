OPENAI_API_KEY=
OPENAI_AGENT_MODEL=gpt-4.1-mini
DATABASE_URL=postgresql://othram:othram@127.0.0.1:5432/othram
# Required by `pnpm eval`; must be a separate database whose name contains `eval`.
EVAL_DATABASE_URL=postgresql://othram:othram@127.0.0.1:5432/othram_eval
# Optional live Local Ticket System development only; `pnpm eval` leaves this false.
LOCAL_TICKET_POLLING_ENABLED=false
LOCAL_TICKET_POLL_INTERVAL_MS=30000
# Deferred voice feasibility configuration; not used by the accepted local ticket workflow.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
# Future administrator-owned Zendesk recovery (OTHRM-29); not used by the accepted workflow.
ZENDESK_SUBDOMAIN=
ZENDESK_CLIENT_ID=
ZENDESK_CLIENT_SECRET=
